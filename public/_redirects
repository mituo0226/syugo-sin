# Cloudflare Pages リダイレクト設定
# SPAモードを無効にして、実際のファイルを配信する

# すべてのHTMLファイルを直接配信
/*.html /:splat.html 200

# その他の静的ファイルも直接配信
/photo/* /photo/:splat 200
/js/* /js/:splat 200
/rule/* /rule/:splat 200
/admin/* /admin/:splat 200
/consult/* /consult/:splat 200
/gofu/* /gofu/:splat 200
/shop/* /shop/:splat 200

# ルートパスはindex.htmlに
/ /index.html 200

# 404エラーの場合はindex.htmlにフォールバック
/* /index.html 404
